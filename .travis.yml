language: python
python:
  - 2.6
  - 2.7
before_install:
  - sudo apt-get build-dep python-psycopg2
notifications:
  irc:
    channels: "irc.freenode.org#nens"
    on_success: change
  email: false
install:
  - pip install psycopg2
  - python bootstrap.py
  - bin/buildout
script:
  - bin/test
